sudo: required

services: docker

before_install:
  - docker-compose up --build -d
  
script:
  - docker-compose run \
    -e DJANGO_SETTINGS_MODULE=djangodocker.settings.testing \
    --no-deps --rm web python3 manage.py test